(function (w) {

    function Base( options ) {
        this.ctx = options.ctx;
        this.img = options.img;
        this.x = options.x;
        this.y = options.y;
    }

    w.Base = Base;
}(window));